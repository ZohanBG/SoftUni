function solve() {
    let buttonElement = document.querySelector("button");
    console.log(buttonElement);
    buttonElement.addEventListener("click", (e)=> {
        e.preventDefault();
        let lectureElement = document.getElementsByClassName("modules")[0];
        
    })
};